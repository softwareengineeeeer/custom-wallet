import { css } from "@emotion/react";

const styles = {
  customButton: css({
    fontFamily: "'Montserrat Variable', sans-serif",
    borderRadius: "12px",
    padding: "12px 24px",
  }),
};

export default styles;
