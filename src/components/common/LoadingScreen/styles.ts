import { css } from "@emotion/react";

const styles = {
  container: css({
    display: "flex",
    alignItems: "center",
    justifyContent: "center",

    flex: "1 0 100%",
  }),
};

export default styles;
